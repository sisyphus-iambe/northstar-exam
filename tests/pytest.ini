# 测试专用配置 (tests/ 内, 不影响北极星任何现有代码).
# langsmith 的 pytest 插件随 --system-site-packages venv 的用户 site-packages
# 自动加载且依赖缺失的 pydantic, 收集阶段崩溃. pytest 的 -p no: 无法阻止
# entrypoint 自动加载 (已知行为), 必须 --disable-plugin-autoload (pytest 9.1+).
[pytest]
addopts = --disable-plugin-autoload
testpaths = .
